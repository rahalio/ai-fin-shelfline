/**
 * OriginationRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: originations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * OriginationRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements OriginationRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  OriginationRepository,
} from "@shelfline/services/originations";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { OriginationRepositoryDdb } from "./origination-repository.ddb.js";

/**
 * OriginationRepositoryAdapter
 *
 * Implements OriginationRepository port for infrastructure layer.
 */
export class OriginationRepositoryAdapter implements OriginationRepository {
  private readonly ddb: OriginationRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new OriginationRepositoryDdb(this.dynamoClient);
  }

  async listOriginations(input: Parameters<OriginationRepository['listOriginations']>[0]): Promise<Awaited<ReturnType<OriginationRepository['listOriginations']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listOriginations(input);
  }

  async submitOrigination(input: Parameters<OriginationRepository['submitOrigination']>[0]): Promise<Awaited<ReturnType<OriginationRepository['submitOrigination']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.submitOrigination(input);
  }

  async getOrigination(input: Parameters<OriginationRepository['getOrigination']>[0]): Promise<Awaited<ReturnType<OriginationRepository['getOrigination']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getOrigination(input);
  }
}
