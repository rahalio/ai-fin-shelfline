/**
 * ForceRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: throttles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ForceRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements ForceRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  ForceRepository,
} from "@shelfline/services/throttles";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { ForceRepositoryDdb } from "./force-repository.ddb.js";

/**
 * ForceRepositoryAdapter
 *
 * Implements ForceRepository port for infrastructure layer.
 */
export class ForceRepositoryAdapter implements ForceRepository {
  private readonly ddb: ForceRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new ForceRepositoryDdb(this.dynamoClient);
  }

  async forceThrottle(input: Parameters<ForceRepository['forceThrottle']>[0]): Promise<Awaited<ReturnType<ForceRepository['forceThrottle']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.forceThrottle(input);
  }
}
