/**
 * ExecuteListProductSkus - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: skus
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListProductSkus Use Case
 *
 * DDD: Application use case for list Sku.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListProductSkusInput, ListProductSkusOutput } from "../dto/sku.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@shelfline/services/_shared/index.js";
import type { SkuRepository } from "../ports/index.js";

export class ExecuteListProductSkus {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly sku: SkuRepository
  ) {}

  async execute(input: ListProductSkusInput): Promise<ListProductSkusOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.skuId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.sku.listProductSkus({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListProductSkusOutput;
  }
}
