/**
 * ExecuteUpsertSkuCapitalConstraint - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: skus
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteUpsertSkuCapitalConstraint Use Case
 *
 * DDD: Application use case for get CapitalConstraint.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { UpsertSkuCapitalConstraintInput, UpsertSkuCapitalConstraintOutput } from "../dto/capital-constraint.dto.js";
import type { ExecutionContextService, IdGeneratorService } from "@shelfline/services/_shared/index.js";
import type { CapitalConstraintRepository } from "../ports/index.js";
import { NotFoundError, ValidationError } from "../errors/index.js";

export class ExecuteUpsertSkuCapitalConstraint {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly capitalConstraint: CapitalConstraintRepository
  ) {}

  async execute(input: UpsertSkuCapitalConstraintInput): Promise<UpsertSkuCapitalConstraintOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.skuId();

    // All field names extracted from OpenAPI spec: currency, maxVolume, rwaWeightHint
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).skuId) {
      throw new ValidationError("skuId is required");
    }
    const skuId = (input as any).skuId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.capitalConstraint.upsertSkuCapitalConstraint({ skuId: skuId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${skuId}`);
    }

    return entity as UpsertSkuCapitalConstraintOutput;
  }
}
