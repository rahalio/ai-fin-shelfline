/**
 * Skus DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: skus
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Skus Domain Module - Composition root
 *
 * DDD: Composition root for skus domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  CapitalConstraintRepositoryAdapter,
  PauseRepositoryAdapter,
  RateCardRepositoryAdapter,
  ResumeRepositoryAdapter,
  RetireRepositoryAdapter,
  SkuRepositoryAdapter,
} from "@shelfline/adapters/skus";
import { getIdGeneratorService } from "@shelfline/adapters";
import type { AdapterDynamoDBClient } from "@shelfline/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateProductSku,
  ExecuteGetProductSku,
  ExecuteGetSkuCapitalConstraint,
  ExecuteListProductSkus,
  ExecuteListSkuRateCards,
  ExecuteUpdateProductSku,
  ExecuteUpsertRateCard,
  ExecuteUpsertSkuCapitalConstraint,
} from "@shelfline/services/skus/usecases";
// Ports (interfaces)
import type {
  CapitalConstraintRepository,
  PauseRepository,
  RateCardRepository,
  ResumeRepository,
  RetireRepository,
  SkuRepository,
} from "@shelfline/services/skus/ports";

export interface SkusDomainModule {
  repos: {
    capitalConstraints: CapitalConstraintRepository;
    pauses: PauseRepository;
    rateCards: RateCardRepository;
    resumes: ResumeRepository;
    retires: RetireRepository;
    skus: SkuRepository;
  };
  useCases: {
    capitalConstraints: {
      get: ExecuteUpsertSkuCapitalConstraint;
    };
    rateCards: {
      get: ExecuteUpsertRateCard;
      list: ExecuteListSkuRateCards;
    };
    skus: {
      create: ExecuteCreateProductSku;
      get: ExecuteGetProductSku;
      list: ExecuteListProductSkus;
      update: ExecuteUpdateProductSku;
    };
  };
}

export function buildSkusDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): SkusDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      capitalConstraints?: CapitalConstraintRepository;
      pauses?: PauseRepository;
      rateCards?: RateCardRepository;
      resumes?: ResumeRepository;
      retires?: RetireRepository;
      skus?: SkuRepository;
    } = {};

    obj.capitalConstraints = new CapitalConstraintRepositoryAdapter(dynamoClient);
    obj.pauses = new PauseRepositoryAdapter(dynamoClient);
    obj.rateCards = new RateCardRepositoryAdapter(dynamoClient);
    obj.resumes = new ResumeRepositoryAdapter(dynamoClient);
    obj.retires = new RetireRepositoryAdapter(dynamoClient);
    obj.skus = new SkuRepositoryAdapter(dynamoClient);
    return {
      capitalConstraints: obj.capitalConstraints!,
      pauses: obj.pauses!,
      rateCards: obj.rateCards!,
      resumes: obj.resumes!,
      retires: obj.retires!,
      skus: obj.skus!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    capitalConstraints: {
      get: new ExecuteUpsertSkuCapitalConstraint(executionContext, idGenerator, repos.capitalConstraints),
    },
    rateCards: {
      get: new ExecuteUpsertRateCard(executionContext, idGenerator, repos.rateCards),
      list: new ExecuteListSkuRateCards(executionContext, idGenerator, repos.rateCards),
    },
    skus: {
      create: new ExecuteCreateProductSku(executionContext, idGenerator, repos.skus),
      get: new ExecuteGetProductSku(executionContext, idGenerator, repos.skus),
      list: new ExecuteListProductSkus(executionContext, idGenerator, repos.skus),
      update: new ExecuteUpdateProductSku(executionContext, idGenerator, repos.skus),
    },
  };
  return { repos, useCases: useCases as SkusDomainModule["useCases"] };
}
