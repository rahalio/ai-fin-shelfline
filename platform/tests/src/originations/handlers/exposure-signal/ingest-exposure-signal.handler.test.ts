/**
 * IngestExposureSignal handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: originations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { ingestExposureSignal } from "@shelfline/api-server/domains/originations/handlers";
import type { OriginationsDDDDependencies } from "@shelfline/api-server/domains/originations";
import { createExposureSignal as createExposureSignalEntity } from "@shelfline/factories/originations";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shelfline/tests/shared";
import { ORIGINATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("IngestExposureSignal", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: OriginationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<OriginationsDDDDependencies>(
      ORIGINATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createExposureSignalEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeIngestExposureSignal, "execute").mockResolvedValue(useCaseResult);

    await ingestExposureSignal(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeIngestExposureSignal.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
