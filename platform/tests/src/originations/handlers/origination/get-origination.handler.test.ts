/**
 * GetOrigination handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: originations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getOrigination } from "@shelfline/api-server/domains/originations/handlers";
import type { OriginationsDDDDependencies } from "@shelfline/api-server/domains/originations";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shelfline/tests/shared";
import { ORIGINATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetOrigination", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: OriginationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<OriginationsDDDDependencies>(
      ORIGINATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetOrigination, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { applicationId: entity.id };

    await getOrigination(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetOrigination.execute).toHaveBeenCalledWith(expect.objectContaining({ applicationId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { applicationId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetOrigination, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getOrigination(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
