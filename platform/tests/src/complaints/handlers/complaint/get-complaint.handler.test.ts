/**
 * GetComplaint handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: complaints
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getComplaint } from "@shelfline/api-server/domains/complaints/handlers";
import type { ComplaintsDDDDependencies } from "@shelfline/api-server/domains/complaints";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shelfline/tests/shared";
import { COMPLAINTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetComplaint", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ComplaintsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ComplaintsDDDDependencies>(
      COMPLAINTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetComplaint, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { complaintId: entity.id };

    await getComplaint(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetComplaint.execute).toHaveBeenCalledWith(expect.objectContaining({ complaintId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { complaintId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetComplaint, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getComplaint(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
