/**
 * UpsertSkuCapitalConstraint handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: skus
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { upsertSkuCapitalConstraint } from "@shelfline/api-server/domains/skus/handlers";
import type { SkusDDDDependencies } from "@shelfline/api-server/domains/skus";
import { createCapitalConstraint as createCapitalConstraintEntity } from "@shelfline/factories/skus";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@shelfline/tests/shared";
import { SKUS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpsertSkuCapitalConstraint", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SkusDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SkusDDDDependencies>(
      SKUS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createCapitalConstraintEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { skuId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpsertSkuCapitalConstraint, "execute").mockResolvedValue(useCaseResult);

    await upsertSkuCapitalConstraint(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpsertSkuCapitalConstraint.execute).toHaveBeenCalledWith(expect.objectContaining({ skuId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
